import { Marketplace } from '@/components/marketplace/marketplace';

export const metadata = {
  title: 'Marketplace',
  description: 'Subscribe to automations other people have already built.',
};

export default function MarketplacePage() {
  return <Marketplace />;
}
