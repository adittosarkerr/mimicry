import { Dashboard } from '@/components/dashboard/dashboard';

export const metadata = {
  title: 'Dashboard',
  description: 'Your automations, runs, usage and billing.',
};

export default function DashboardPage() {
  return <Dashboard />;
}
