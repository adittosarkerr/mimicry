import { VoiceStudio } from '@/components/voice/voice-studio';

export const metadata = {
  title: 'Voice',
  description: 'Say what you need and Mimic runs the automation for you.',
};

export default function VoicePage() {
  return <VoiceStudio />;
}
